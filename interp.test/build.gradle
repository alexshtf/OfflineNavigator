apply plugin: 'java'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'org.hamcrest:hamcrest-all:1.3'
    compile 'junit:junit:4.12'
    compile project(':interp')
}